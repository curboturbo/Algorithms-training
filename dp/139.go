package main
///class Solution:
///    def wordBreak(self, s: str, wordDict: list[str]) -> bool:
///        d =dict()
///        dp = [False for i in range(len(s)+1)]
///        dp[0] = True
///        for i in wordDict:
///            d[i] = 1
///        for i in range(0,len(s)+1):
///            for pattern in wordDict:
///                start_position = i - len(pattern)
///                if start_position >=0 and dp[start_position] and d[s[start_position:i]] == 1:
//                    dp[i] = True
///        return dp[-1]
    


func wordBreak(s string, wordDict []string) string {
	python_solution:="ass"
	return python_solution
}