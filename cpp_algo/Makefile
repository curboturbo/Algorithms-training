# Получаем список всех файлов с расширением .cpp
SOURCES := $(wildcard *.cpp)

# Создаём список имён исполняемых файлов, удаляя расширение .cpp
TARGETS := $(patsubst %.cpp, %, $(SOURCES))

# Правило по умолчанию, которое собирает все цели (исполняемые файлы)
all: $(TARGETS)

# Шаблон правила: как скомпилировать каждый файл .cpp
%: %.cpp
	g++ -o $@ $<

# Правило для очистки: удаляет все скомпилированные исполняемые файлы
.PHONY: clean
clean:
	rm -f $(TARGETS)